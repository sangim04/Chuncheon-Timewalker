using UnityEngine;

public class sdfsdfsdf : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("sjdlfjeisldf");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
